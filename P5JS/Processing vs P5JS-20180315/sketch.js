
// STEP 1: Print text to the console
console.log("Hello console!");


/*
// STEP 2: the setup function:
// we use the word function (instead of void) to write a function in javaScript.
function setup() {
	// write something to the console
	console.log("The good 'ol setup() function!");
	// instead of size() we use createCanvas() to create our canvas
	createCanvas(400,300);
	// change background as usual
	background(255,0,0);
}
*/



/*
// STEP 3: the draw function:
// instead of void draw(), we write function draw()
function draw() {
	background(random(255),0,0);
}
*/



/*
// STEP 4: All kinds of other pre-defined functions
function mousePressed() {	
}
function mouseDragged() {	
}
function mouseReleased() {	
}
function mouseClicked() {	
}
function doubleClicked() {	
}
//etc... @see https://p5js.org/reference/
*/